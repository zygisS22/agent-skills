# Agent Skills

Public Agent Skills for coding agents.

## Skills

- `html-artifacts`: Create self-contained HTML artifacts when agent output is easier to review visually or interactively than Markdown.

## Install

Install the HTML artifacts skill:

```bash
npx skills add zygisS22/agent-skills --skill html-artifacts
```

Or install it from the direct skill path:

```bash
npx skills add https://github.com/zygisS22/agent-skills/tree/main/skills/html-artifacts
```

## Layout

```text
skills/
  html-artifacts/
    SKILL.md
    LICENSE
```
